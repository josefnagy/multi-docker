import { describe, it, expect } from "vitest";

describe("Initial test", () => {
    it("runs a test", () => {
        expect(1).toBeTruthy();
    });
    it("runs another test", () => {
        expect(2).toBeTruthy();
    });
});
